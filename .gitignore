# macOS
.DS_Store

# User secrets
.env
.env.local

# User runtime data — these are generated by the plugin in $DAILY_PLANNER_HOME,
# not part of the plugin source. If you're using this repo as your data dir,
# remove these from .gitignore (and accept that your journal is in your repo).
today.md
week.md
days/

# Editors
.vscode/
.idea/
*.swp
